spring:
  application:
    name: pullwise-api

  datasource:
    url: jdbc:postgresql://192.168.1.110:5444/pullwise?ssl=false&serverTimezone=America/Sao_Paulo
    username: admin
    password: Rapidex@727204

  jpa:
    hibernate:
      ddl-auto: update
    show_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true

  # External Redis
  data:
    redis:
      host: 192.168.1.110
      port: 6379
      password:
      database: 0
      timeout: 5000

  # Artemis/ActiveMQ
  activemq:
    broker-url: tcp://192.168.1.110:61616
    user: admin
    password: admin

logging:
  level:
    com.pullwise: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
